/**
 * Global Configuration
 * This file defines BASE_URL dynamically for network access
 * It should be included before all other JavaScript files
 */
var BASE_URL = window.location.origin + '/';
console.log("BASE_URL" + BASE_URL);
